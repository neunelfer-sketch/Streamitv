<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!--
      Media3 legt für die Medien-Session eine Benachrichtigung an, sobald der
      Dienst in den Vordergrund geht. Ab Android 13 ist dafür dieses Recht
      nötig. Auf einem Fernseher bekommt sie ohnehin niemand zu sehen – ohne
      die Angabe kann der Wechsel in den Vordergrund aber scheitern, und
      damit fiele die Session weg, an der das System "hier läuft etwas"
      erkennt.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      Für die eingebaute Aktualisierung: Die App lädt die neue Fassung
      selbst herunter und übergibt sie dem System-Installationsprogramm.
      Ab Android 8 muss das Recht zusätzlich vom Nutzer bestätigt werden
      ("Apps aus unbekannten Quellen") – die App führt ihn bei Bedarf
      direkt dorthin, siehe UpdateRepository.
    -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!--
      "android.software.leanback" sorgt dafür, dass die App im
      Google-Play-/Amazon-TV-Store als TV-App erkannt wird. Bewusst
      required=false statt true: Die App läuft inzwischen auch auf
      Handys (Touch-Bedienung, freie Bildschirmausrichtung) - required=true
      würde sie dort als inkompatibel markieren und vom Store ausblenden.
    -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />

    <application
        android:name=".NeunelfPlayerApplication"
        android:allowBackup="true"
        android:banner="@drawable/app_banner"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.NeunelfPlayer"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:configChanges="keyboard|keyboardHidden|navigation|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:exported="true"
            android:launchMode="singleTask"
            android:resizeableActivity="true"
            android:supportsPictureInPicture="true"
            android:theme="@style/Theme.NeunelfPlayer">

            <!-- Startpunkt auf Android TV / Fire TV (Leanback-Launcher) -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>

            <!-- Zusätzlich im normalen Launcher sichtbar (Tablets, Emulator) -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Reicht die heruntergeladene APK an das Installationsprogramm
          weiter. Ab Android 7 lehnt das System `file://`-Adressen zwischen
          Apps ab; der Umweg über einen FileProvider ist der vorgesehene Weg.
          Die Kennung trägt die Anwendungs-ID, damit sich Test- und
          Release-Build nicht in die Quere kommen.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.updates"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Hintergrund-Wiedergabe / Medien-Session für PiP und TV-Fernbedienungstasten -->
        <service
            android:name=".player.PlaybackService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>
    </application>
</manifest>
